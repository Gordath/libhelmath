sudo: required
dist: precise
language: cpp
compiler:
    - gcc

addons:
    apt:
        sources:
            - llvm-toolchain-precise
            - ubuntu-toolchain-r-test
        packages:
            - clang-3.7
            - g++-5
            - gcc-5
            - libffi-dev
            - libssl-dev

before_install:
   - sudo apt-get update

install:
    - sudo apt-get install lcov rubygems
    - sudo gem install lcoveralls
    - sudo pip install requests[security]
    - sudo apt-get install lcov
    - if [ "$CXX" = "g++" ]; then export CXX="g++-5" CC="gcc-5"; fi

before_script:
    - gcov -version
    - lcov -v
    - gcc -v
    - g++ -v
    - mkdir build
    - cd build
    - cmake -DCOVERAGE=ON ..

script:
    - make
    - cd tests
    - ./all_tests
    - cd CMakeFiles/all_tests.dir/
    - mv matrix/matrix2/*.*.gc* .
#    - find . -type f -iname "*.gc*" -exec mv {} . \;
    - lcov -c -d . -o all_tests.info

after_success:
  - lcoveralls --root .. -v

notifications:
  recipients:
    - agelosbb@gmail.com
  email:
    on_success: always
    on_failure: always